<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Dynamic Data - Cloud Data</title>
    <link rel="shortcut icon" type="image/jpg" href="/favicon.ico" />
    <!-- Google Font: Source Sans Pro -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700&display=fallback">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="~/plugins/fontawesome-free/css/all.min.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="~/dist/css/adminlte.min.css">
    <link href="~/plugins/toastr/toastr.css" rel="stylesheet" />
    <link href="~/dist/css/jquery.contextMenu.css" rel="stylesheet" />

    <link href="~/plugins/datatables-bs4/css/dataTables.bootstrap4.css" rel="stylesheet" />
    <link href="~/plugins/datatables-buttons/css/buttons.bootstrap4.css" rel="stylesheet" />
    <link href="~/plugins/datatables-select/css/select.bootstrap4.css" rel="stylesheet" />
    <link href="~/plugins/datatables-keytable/css/keyTable.bootstrap4.css" rel="stylesheet" />
    <link href="~/plugins/datatables/css/editor.bootstrap4.css" rel="stylesheet" />
    <link href="~/css/addition.css" rel="stylesheet" />
    <style>
        .brand-logo {
            display: block;
            font-size: 1.7rem;
            font-weight: bolder;
            line-height: 1.75;
            /*   padding: 0.8125rem 0.5rem;*/
            transition: width 0.3s ease-in-out;
            white-space: nowrap;
        }

        .dataTables_scrollHead {
            overflow: inherit !important;
        }

        .DTE_Form_Content select {
            height: 45px;
        }
    </style>
</head>
<body class="hold-transition sidebar-mini">
    <!-- Site wrapper -->
    <div class="wrapper">
        <!-- Navbar -->
        <nav class="main-header navbar navbar-expand navbar-white navbar-light pull-right">
            <!-- Left navbar links -->
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
                </li>

                <li class="nav-item d-none d-sm-inline-block dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Admin
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="@Url.Action("Users","Admin")"><i class="fas fa-user-circle"></i> Users</a>
                        <a class="dropdown-item" href="@Url.Action("Roles","Admin")"><i class="fas fa-users-cog "></i> Roles</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="@Url.Action("Settings","Admin")"><i class="fas fa-cog "></i> Settings</a>
                    </div>
                </li>
            </ul>

            <!-- SEARCH FORM -->
            <!-- Right navbar links -->
            <ul class="navbar-nav ml-auto">

                <li class="nav-item">
                    <div class="sidebar-shortcuts-large" id="sidebar-shortcuts-large">
                        <button class="btn btn-sm btn-success" title="New Library" onclick="showAddModel('@Url.Action("ActionLibrary","Home")',0, 'mCreateLibrary', 'formLibrary',CreateGuid())">
                            <i class="ace-icon fa fa-tasks"></i>
                        </button>
                        <button class="btn btn-sm btn-warning" title="Add Groups" data-toggle="modal" data-target="#exampleModal">
                            <i class="ace-icon fa fa-users"></i>
                        </button>
                        <button class="btn btn-sm btn-danger" title="Permissions">
                            <i class="ace-icon fa fa-lock"></i>
                        </button>
                        <button class="btn btn-sm btn-warning" title="Setting">
                            <i class="ace-icon fa fa-cogs"></i>
                        </button>
                    </div>
                </li>
            </ul>
        </nav>
        <!-- /.navbar -->
        <!-- Main Sidebar Container -->
        <aside class="main-sidebar sidebar-dark-primary elevation-4">
            <!-- Brand Logo -->
            <a href="/" class="brand-logo" style="text-align: left;background-color:white">
                <img src="~/dist/img/logo2.png" width="60" />
                &nbsp;<span class="brand-text font-weight-light"> Dynamic Data</span>
            </a>

            <!-- Sidebar -->
            <div class="sidebar">
                <!-- Sidebar user (optional) -->
                <!-- Sidebar Menu -->
                <nav class="mt-2">

                    <ul class="nav nav-pills nav-sidebar flex-column nav-child-indent text-sm" data-widget="treeview" role="menu" data-accordion="false" id="ulSideMenu">
                        @Html.RenderAction("_SideMenu", "Home")
                    </ul>
                </nav>
                <!-- /.sidebar-menu -->
            </div>
            <!-- /.sidebar -->
        </aside>

        <!-- Content Wrapper. Contains page content -->
        <div class="content-wrapper">
            <!-- Content Header (Page header) -->
            @*<section class="content-header">
                    <div class="container-fluid">
                        <div class="row mb-2">
                            <div class="col-sm-6">
                                <h1>Welcome</h1>
                            </div>
                            <div class="col-sm-6">
                                <ol class="breadcrumb float-sm-right">
                                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                                    <li class="breadcrumb-item active">Welcome</li>
                                </ol>
                            </div>
                        </div>
                    </div> /.container-fluid
                </section>*@

            <!-- Main content -->
            <section class="content">
                <br />
                @RenderBody()
                <!-- Default box -->


            </section>
            <!-- /.content -->
        </div>
        <!-- /.content-wrapper -->

        <footer class="main-footer">
            <div class="float-right d-none d-sm-block">
                <b>Version</b> 1.0.0
            </div>
            <strong>Copyright &copy; 2014-@DateTime.Now.Year</strong>. Site Developed by <a href="#">Danny Pham</a>. All rights reserved. Bootstrap Template By Admin LTE v3.1.0
        </footer>

        <!-- Control Sidebar -->
        <aside class="control-sidebar control-sidebar-dark">
            <!-- Control sidebar content goes here -->
        </aside>
        <!-- /.control-sidebar -->
    </div>
    <div class="modal fade" id="mCreateLibrary" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">

    </div>

    <div class="modal fade" id="modelConfirm" aria-modal="true" data-backdrop="static" data-keyboard="false">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">Comfirmation</h4>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body" id="mConfirmBody">
                    <p>Are you sure you want to  delete?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-danger" data-dismiss="modal" onclick="doDeleteLibrary()">Yes</button>
                    <button type="button" class="btn btn-outline-primary" data-dismiss="modal">No</button>
                </div>
            </div>
            <!-- /.modal-content -->
        </div>
        <!-- /.modal-dialog -->
    </div>
    <!-- ./wrapper -->
    <!-- jQuery -->
    <script src="~/plugins/jquery/jquery.min.js"></script>
    <script src="~/plugins/jquery-ui/jquery-ui.js"></script>
    <!-- Bootstrap 4 -->
    <script src="~/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <!-- AdminLTE App -->
    <script src="~/dist/js/adminlte.min.js"></script>
    <!-- AdminLTE for demo purposes -->
    <script src="~/dist/js/demo.js"></script>
    <script src="~/plugins/jquery-validation/jquery.validate.js"></script>
    <script src="~/plugins/toastr/toastr.min.js"></script>
    <script src="~/plugins/bootstrap-contextmenu/bootstrap-contextmenu.js"></script>
    <script src="~/plugins/datatables/jquery.dataTables.js"></script>
    <script src="~/plugins/datatables-bs4/js/dataTables.bootstrap4.js"></script>
    <script src="~/plugins/datatables-buttons/js/dataTables.buttons.js"></script>
    <script src="~/plugins/datatables-select/js/dataTables.select.js"></script>
    <script src="~/plugins/datatables-keytable/js/dataTables.keyTable.js"></script>
    <script src="~/plugins/moment/moment.min.js"></script>
    <script src="~/plugins/moment/datetime-moment.js"></script>
    <script src="~/plugins/datatables/dataTables.editor.js"></script>
    <script src="~/plugins/datatables/editor.bootstrap4.js"></script>
    <script src="~/plugins/datatables/dataTables.fixedHeader.min.js"></script>
    <script src="~/plugins/datatables/dataTables.colReorder.min.js"></script>
    <script src="~/plugins/datatables/dataTables.rowGroup.min.js"></script>
    <script src="~/plugins/datatables/jszip.min.js"></script>
    <script src="~/plugins/pdfmake/pdfmake.js"></script>
    <script src="~/plugins/pdfmake/vfs_fonts.js"></script>
    <script src="~/plugins/datatables/buttons.html5.min.js"></script>
    <script src="~/plugins/datatables/buttons.print.min.js"></script>
    <script src="~/js/accountant/accounting.min.js"></script>
    <style>
        .selectedmenu {
            background-color: gray;
        }
    </style>

    <script>
        toastr.options = {
            "closeButton": false,
            "debug": false,
            "newestOnTop": false,
            "progressBar": true,
            "positionClass": "toast-top-center",
            "preventDuplicates": false,
            "onclick": null,
            "showDuration": "600",
            "hideDuration": "1000",
            "timeOut": "5000",
            "extendedTimeOut": "1000",
            "showEasing": "swing",
            "hideEasing": "linear",
            "showMethod": "fadeIn",
            "hideMethod": "fadeOut"
        }

        accounting.settings = {
            currency: {
                symbol: "$",   // default currency symbol is '$'
                format: "%s%v", // controls output: %s = symbol, %v = value/number (can be object: see below)
                decimal: ".",  // decimal point separator
                thousand: ",",  // thousands separator
                precision: 2   // decimal places
            },
            number: {
                precision: 0,  // default precision on numbers is 0
                thousand: ",",
                decimal: "."
            }
        }

        accounting.settings.currency.format = {
            pos: "%s %v",   // for positive values, eg. "$ 1.00" (required)
            neg: "%s (%v)", // for negative values, eg. "$ (1.00)" [optional]
            zero: "%s  0.00 "  // for zero values, eg. "$  --" [optional]
        };

        $(".show-modal").click(function () {
            $("#mFieldAction").modal({
                backdrop: 'static',
                keyboard: false
            });
        });

        function navRedirect(controller, Guid) {
            document.location = "/" + controller + "/Index/" + Guid;
        }
        var selectLibraryGuid;
        $(document).ready(function () {
            initialSortable();
            $.contextMenu({
                selector: '.nav-item',
                build: function ($trigger, e) {
                    selectLibraryGuid = e.currentTarget.id;
                    return {
                        callback: function (key, options) {
                            if (key == "edit") {
                                viewEditLibrary();
                            }
                             if (key == "add") {
                                viewAddLibrary();
                             }
                            if (key == 'delete') {
                               viewDeleteLibrary();
                            }
                        },
                        items: {
                            "edit": { name: "Edit Library", icon: "edit" },
                            "add": { name: "Add Library", icon: "add" },
                            "delete": { name: "Delete Library", icon: "delete" },
                            "sep1": "---------",
                            "cancel": { name: "Cancel", icon: function ($element, key, item) { return 'context-menu-icon context-menu-icon-quit'; } }
                        }
                    };
                }
            });

            var libraryFieldTitle;


        });

        $(document).ajaxSuccess(function () {
            initialSortable();
        });

        function initialSortable() {
            $(".nav-treeview").sortable({
                start: function (event, ui) {

                },
                stop: function (event, ui) {
                    var attr = $(ui.item).closest("ul");
                    attr.each(function () {
                        var localRels = [];
                        $(this).find('li').each(function () {
                            localRels.push($(this).attr('id'));
                        });
                        $.post('@Url.Action("SaveLibraryOrder","Home")', {
                            ids: localRels
                        });
                    });

                    var data = "";
                    $(".nav-treeview li").each(function (i, el) {
                        var p = $(el).text().toLowerCase().replace(" ", "_");
                        data += p + "=" + $(el).index() + ",";
                    });
                    console.log(data);
                }
            });
        }

        var formValidate;
        function showAddModel(url,libGuid, modalId, formID, parentID) {
            $.get(url, { guid: libGuid, parentGuid: parentID}, function (res) {
                console.log(res);
                $("#" + modalId).html(res);
                $("#" + formID).validate(
                    {
                        errorPlacement: function (error, element) {
                            element.parent().find('.text-danger').text(error.text());
                        },
                        highlight: function (element, errorClass, validClass) {

                            $(element).addClass('has-error');
                        },
                        unhighlight: function (element, errorClass, validClass) {
                            $(element).removeClass('has-error');
                            $(element).parent().find('.text-danger').text("");
                        }
                    });
                $("#" + modalId).modal("show");
            });
        }

        function libraryFormAction(form,modalId) {
            var form = $(form);
            form.validate();
            if (form.valid()) {
                $.ajax(
                    {
                        url: '@Url.Action("ActionLibrary", "Home")',
                        type: 'POST',
                        data: $(form).serialize(),
                        success: function (res) {
                            if (res.success) {
                                $("#" + modalId).modal("hide");
                                var url = '@Url.Action("_SideMenu", "Home")';
                                if (selectLibraryGuid === undefined)
                                    selectLibraryGuid = CreateGuid();
                                $("#ulSideMenu").load(url + "?guid=" + selectLibraryGuid);
                                toastr.success(res.message);
                            }
                            else {
                                toastr.error(res.message);
                            }
                        }
                    }
                )
            }
            return false;
        }

        function viewAddLibrary() {
           // console.log(selectLibraryGuid);
           showAddModel('@Url.Action("ActionLibrary","Home")',0, 'mCreateLibrary', 'formLibrary', selectLibraryGuid)
        }

        function viewEditLibrary() {
            //console.log(selectLibraryGuid);
            showAddModel('@Url.Action("ActionLibrary","Home")', selectLibraryGuid,'mCreateLibrary', 'formLibrary', selectLibraryGuid)
        }

        function viewDeleteLibrary() {
            $("#modelConfirm").modal("show");
        }

        function doDeleteLibrary() {
            //console.log($($("#" + selectLibraryGuid).closest("ul").closest("li"))[0].id);
            $.get('@Url.Action("DeleteLibrary","Home")', { guid: selectLibraryGuid }, function (res) {
                if (res.success) {
                    toastr.success(res.message);
                    var url = '@Url.Action("_SideMenu", "Home")';
                    var parentNode;
                    if (selectLibraryGuid === undefined)
                        parentNode = CreateGuid();
                    else {
                        if ($($("#" + selectLibraryGuid).closest("ul").closest("li"))[0] !== undefined)
                            parentNode = $($("#" + selectLibraryGuid).closest("ul").closest("li"))[0].id;
                        else
                            parentNode = CreateGuid();
                    }
                    $("#ulSideMenu").load(url + "?guid=" + parentNode);
                }
                else {
                    toastr.error(res.message);
                }
            });
        }

        function CreateGuid() {
            function _p8(s) {
                var p = (Math.random().toString(16) + "000000000").substr(2, 8);
                return s ? "-" + p.substr(0, 4) + "-" + p.substr(4, 4) : p;
            }
            return _p8() + _p8(true) + _p8(true) + _p8();
        }


    </script>


    @RenderSection("Scripts", required: false)
</body>
</html>
